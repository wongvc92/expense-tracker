import { DatePickerWithRange } from "../ui/date-picker-with-range";

const TransactionDateFilter = () => {
  return (
    <div>
      <DatePickerWithRange title="Date" />
    </div>
  );
};

export default TransactionDateFilter;
